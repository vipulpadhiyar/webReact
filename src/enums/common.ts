/* defined all app common enums */

export enum FilePickerType {
  CAMERA,
  GALLERY,
  CANCEL,
}

export enum Role {
  ADMIN = 'Admin',
  HO = 'HO',
  TC = 'TC',
  DRIVER = 'DRIVER',
}

export enum InitialAuthTab {
  SIGN_IN = 'SIGN_IN',
  CREATE_ACCOUNT = 'CREATE_ACCOUNT',
}
/* Enum for creating trip */
export enum CreateTrip {
  ONE_WAY = 'oneWay',
  ROUND = 'roundTrip',
}
/* Enum for platform type  */
export enum PlatformType {
  MOBILE = 'mobile',
}
